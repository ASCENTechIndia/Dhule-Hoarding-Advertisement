// shared-pdf-options.js
const PDF_RENDER_OPTIONS = Object.freeze({
  format: "A4",
  printBackground: true,
  preferCSSPageSize: true,
  margin: { top: "0", bottom: "0", left: "0", right: "0" },
});

module.exports = { PDF_RENDER_OPTIONS };